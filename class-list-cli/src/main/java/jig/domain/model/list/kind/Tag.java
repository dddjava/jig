package jig.domain.model.list.kind;

public enum Tag {
    SERVICE,
    DATASOURCE,
    ENUM,
    ENUM_PARAMETERIZED,
    ENUM_POLYMORPHISM,
    IDENTIFIER,
    NUMBER,
    DATE,
    TERM,
    ENUM_BEHAVIOUR, FIRST_CLASS_COLLECTION
}
