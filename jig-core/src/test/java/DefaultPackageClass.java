/**
 * デフォルトパッケージにあるクラス
 *
 * パッケージ関連図などはパッケージを前提にしている。
 * デフォルトパッケージのクラスが存在した場合に正常動作しなかったことがあったため、
 * そのケースでは本クラスを使用する。
 *
 * {@link testing.TestSupport} ではクラスが存在するパスの検出に使用している。
 */
public class DefaultPackageClass {
}
