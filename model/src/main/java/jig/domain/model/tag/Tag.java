package jig.domain.model.tag;

public enum Tag {
    SERVICE,
    REPOSITORY,
    ENUM,
    ENUM_PARAMETERIZED,
    ENUM_POLYMORPHISM,
    IDENTIFIER,
    NUMBER,
    DATE,
    TERM,
    ENUM_BEHAVIOUR,
    FIRST_CLASS_COLLECTION
}
