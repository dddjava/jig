pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id 'org.dddjava.jig-gradle-plugin' version '2021.5.5'
    }
}

rootProject.name = 'jig'

include 'jig-core',
        'jig-cli',
        'jig-cli-kt',
        'jig-gradle-plugin'
