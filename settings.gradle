pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id 'org.dddjava.jig-gradle-plugin' version '2021.9.2'
    }
}

rootProject.name = 'jig'

include 'jig-core',
        'jig-cli',
        'jig-cli-kt',
        'jig-gradle-plugin'
