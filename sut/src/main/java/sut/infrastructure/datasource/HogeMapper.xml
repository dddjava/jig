<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="sut.infrastructure.datasource.HogeMapper">

    <select id="findOne">
        SELECT * FROM HOGE
    </select>

    <insert id="register">
        INSERT INTO HOGE VALUES(1)
    </insert>
</mapper>